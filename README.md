# 📰 News Platform - Кроссплатформенный новостной агрегатор

Привет! Это полноценное приложение для чтения новостей, которое работает и на мобильных устройствах (iOS/Android), и в браузере. Проект построен на современном стеке технологий и включает все необходимые фичи для комфортной работы с новостным контентом.

## ✨ Основные возможности

- 📱 **Кроссплатформенность** - одна кодовая база для iOS, Android и Web
- 🔍 **Умный поиск** - поиск по ключевым словам с дебаунсом
- 🗂️ **Фильтрация** - по категориям и датам публикации
- ⭐ **Избранное** - сохранение интересных статей офлайн
- 🌐 **WebView** - чтение полных статей прямо в приложении
- 🔐 **Биометрия** - вход через Face ID / Touch ID
- 🔔 **Push-уведомления** - оповещения о новых новостях
- 📎 **Работа с файлами** - загрузка и отправка документов
- ♾️ **Бесконечная прокрутка** - подгрузка новостей по мере скролла
- 🎨 **Современный дизайн** - градиенты, тени, плавные анимации

## 🛠️ Технологический стек

**Mobile:**
- React Native + Expo
- Redux Toolkit + RTK Query
- React Navigation
- NativeWind (Tailwind для RN)
- Expo Local Authentication
- Expo Notifications

**Web:**
- Next.js 16
- Redux Toolkit + RTK Query
- Tailwind CSS
- TypeScript

## 🚀 Быстрый старт

### Установка

Сначала нужно установить все необходимые пакеты. Проект состоит из двух частей - мобильного и веб-приложения:

```bash
# Для мобильного приложения
npm --prefix mobile install

# Для веб-приложения
npm --prefix web install
```

### Запуск проекта

**Мобильное приложение (React Native + Expo):**

```bash
# Запустить dev-сервер
npm --prefix mobile run start

# Или сразу открыть в симуляторе
npm --prefix mobile run ios      # для iOS
npm --prefix mobile run android  # для Android
npm --prefix mobile run web      # для браузерной версии
```

**Веб-приложение (Next.js):**

```bash
# Режим разработки
npm --prefix web run dev

# Сборка для продакшена
npm --prefix web run build
npm --prefix web run start
```

## 🔑 Настройка API ключа

Для работы с новостями используется NewsAPI. Получить бесплатный ключ можно на [newsapi.org](https://newsapi.org).

После получения ключа добавьте его в `mobile/app.json`:

```json
{
  "expo": {
    "extra": {
      "NEWS_API_KEY": "ваш_ключ_здесь"
    }
  }
}
```

Для веб-версии ключ уже прописан в коде (в продакшене лучше использовать переменные окружения).

## 🔔 Push-уведомления

В мобильном приложении реализована поддержка push-уведомлений через Expo Notifications. При запуске приложение запрашивает разрешение на отправку уведомлений и получает токен.

**Как это работает:**
- При первом запуске приложение попросит разрешение на уведомления
- Токен для тестирования выводится в консоль разработчика
- Можно отправить тестовое уведомление через API Expo: `https://exp.host/--/api/v2/push/send`
- При клике на уведомление приложение откроет соответствующую новость

**Важно:** Push-уведомления полноценно работают только на реальных устройствах. В симуляторе и веб-версии функционал ограничен.

## 🔐 Биометрическая аутентификация

Для безопасности приложение использует биометрическую аутентификацию (Face ID / Touch ID). При первом запуске вас встретит экран разблокировки.

**Как пользоваться:**
- Нажмите кнопку "Войти с биометрией"
- Подтвердите вход через Face ID или Touch ID
- После успешной проверки откроется главный экран
- Выйти можно через раздел "Settings" → "Logout"

**Для разработки и тестирования:**

Если вы работаете в симуляторе iOS, нужно включить Face ID:
1. В меню симулятора: **Features → Face ID → Enrolled**
2. При входе в приложение: **Features → Face ID → Matching Face**

Если биометрия недоступна (например, в веб-версии или симуляторе без настройки), появится кнопка "Войти без биометрии (демо)" - это нормально, так задумано для удобства разработки.

## 🏗️ Архитектура проекта

Проект построен по методологии **Feature-Sliced Design** - это помогает держать код организованным и масштабируемым:

```
src/
├── app/          # Точка входа, навигация, глобальный стор
├── entities/     # Бизнес-сущности (избранное, аутентификация)
├── features/     # Фичи приложения
├── pages/        # Экраны приложения
└── shared/       # Переиспользуемый код (API, UI-компоненты, утилиты)
```

**Управление состоянием:**
- Redux Toolkit для глобального стейта
- RTK Query для работы с API (автоматический кэш, ретраи и т.д.)
- AsyncStorage/localStorage для сохранения избранного
- Отдельные слайсы для разных доменов (auth, favorites)

## 🎨 UI/UX особенности

Приложение включает готовые компоненты для разных состояний:
- **ErrorState** - красивое отображение ошибок с кнопкой повтора
- **EmptyState** - информативные пустые состояния
- **NewsCard** - карточки новостей с hover-эффектами
- Прогресс-бары для загрузки файлов
- Скелетоны и лоадеры

## 🧪 Тестирование

Для мобильного приложения настроен Jest:

```bash
npm --prefix mobile run test
```

Покрыты тестами:
- Redux слайсы (favorites, auth)
- Утилиты фильтрации новостей
- Основная бизнес-логика

## 📝 Качество кода

Проект использует ESLint и Prettier для поддержания единого стиля:

```bash
# Проверка кода
npm --prefix mobile run lint
npm --prefix web run lint

# Автоформатирование
npm --prefix mobile run format
npm --prefix web run format
```


## 📱 Скриншоты и демо

Приложение включает следующие экраны:
- **Главная** - лента новостей с фильтрами
- **Детали статьи** - полный просмотр через WebView
- **Избранное** - сохраненные статьи
- **Файлы** - загрузка и скачивание документов
- **Настройки** - выход из аккаунта

## 🤝 Разработка

Проект использует современные практики разработки:
- TypeScript для типобезопасности
- Feature-Sliced Design для масштабируемости
- Redux Toolkit для предсказуемого стейт-менеджмента
- RTK Query для эффективной работы с API
- Tailwind/NativeWind для консистентных стилей

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 💡 Идеи для улучшения

- [ ] Добавить темную тему
- [ ] Реализовать офлайн-режим с кэшированием
- [ ] Добавить возможность делиться статьями
- [ ] Интегрировать больше источников новостей
- [ ] Добавить персонализированные рекомендации
- [ ] Реализовать комментарии к статьям

---

Сделано с ❤️ и современными технологиями
